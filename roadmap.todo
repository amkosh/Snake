//release 0.0.4
    ++bonus item (5000 points)
    ++blocks on field
    ++reverse
    ++levels
    ++levelEditor
    ++editor load map
    ++deleteMap
\\---

//release 0.0.5
+twin snakes
+add phone support
+achievments:
 +uroboros
 +four dimensions
 +speed of light (speed == 0)
